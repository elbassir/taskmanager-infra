# ============================================================
# Secret — Credentials DB (à créer via kubectl ou Terraform)
# NE PAS commiter avec de vraies valeurs !
# ============================================================
#
# Créer via kubectl :
#   kubectl create secret generic taskmanager-db-secret \
#     --namespace taskmanager \
#     --from-literal=DB_URL='jdbc:postgresql://<RDS_ENDPOINT>:5432/taskdb' \
#     --from-literal=DB_USERNAME='taskadmin' \
#     --from-literal=DB_PASSWORD='<PASSWORD>'
#
# Ou via AWS Secrets Manager + External Secrets Operator
# ============================================================
# Ce secret est géré par Terraform (infra/terraform/main.tf)
# Ne pas appliquer ce fichier manuellement — terraform apply s'en charge.
#
# Pour référence, la structure attendue :
#   DB_URL:      jdbc:postgresql://<RDS_ENDPOINT>:5432/taskdb
#   DB_USERNAME: taskadmin
#   DB_PASSWORD: <mot de passe défini dans terraform.tfvars>
#
# apiVersion: v1
# kind: Secret
# metadata:
#   name: taskmanager-db-secret
#   namespace: taskmanager
# type: Opaque
